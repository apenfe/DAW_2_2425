window.addEventListener("DOMContentLoaded", function () {

    let texto = this.prompt("Introduce un texto:");
    let texto2 = this.prompt("Introduce otro texto:");

    this.alert(texto + " " + texto2);

});

